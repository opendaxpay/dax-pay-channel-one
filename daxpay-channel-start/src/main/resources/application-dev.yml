# ====================================================================
# 通道适配子应用 - 开发环境配置
# ====================================================================
# 监控端点全开(无敏感数据, 排障友好; prod 收紧见 application-prod.yml)
management:
  endpoints:
    web:
      exposure:
        include: >
          health,info,httpexchanges,
          metrics,loggers,threaddump,
          beans,mappings,scheduledtasks,
          caches,conditions,startup
  endpoint:
    health:
      show-details: always
# 开发时显示业务包 DEBUG 日志
logging:
  level:
    cn.daxpay.open.**: debug
