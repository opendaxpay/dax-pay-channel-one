<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.0</version>
        <relativePath/>
    </parent>

    <groupId>cn.daxpay.open.channel.one</groupId>
    <artifactId>daxpay-channel-one</artifactId>
    <version>4.0.0-beta3</version>
    <packaging>pom</packaging>
    <name>daxpay-channel-one</name>
    <description>通道适配子应用 - 通道对接服务</description>

    <!-- 开源协议：GNU LGPL v3.0 或更高版本 -->
    <licenses>
        <license>
            <name>GNU Lesser General Public License v3.0 or later</name>
            <url>https://www.gnu.org/licenses/lgpl-3.0.txt</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <modules>
        <module>daxpay-platform</module>
        <module>daxpay-channel-impl</module>
        <module>daxpay-channel-start</module>
    </modules>

    <properties>
        <maven.compiler.source>25</maven.compiler.source>
        <maven.compiler.target>25</maven.compiler.target>
        <java.version>25</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- 三方库版本集中管理(子模块通过继承获取) -->
        <lombok.version>1.18.46</lombok.version>
        <hutool.version>5.8.46</hutool.version>
        <alipay-sdk.version>4.40.272.ALL</alipay-sdk.version>
        <wxjava.version>4.8.4.B</wxjava.version>
        <!-- guava: weixin-java-pay 传递依赖, 显式声明统一版本(排除传递以避免版本漂移) -->
        <guava.version>33.6.0-jre</guava.version>
        <!-- 抖音支付官方 SDK -->
        <douyinpay.version>1.0.6</douyinpay.version>
        <!-- OkHttp: 锁定 4.x (抖音 SDK 引用 okhttp3.internal.Util, 5.x 已移除), 配套 okio 3.6.0 -->
        <okhttp.version>4.12.0</okhttp.version>
        <okio.version>3.6.0</okio.version>
    </properties>

    <!-- 统一锁定版本, 防止 alipay-sdk 等传递旧版 okhttp/okio 造成类冲突 -->
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.squareup.okhttp3</groupId>
                <artifactId>okhttp</artifactId>
                <version>${okhttp.version}</version>
            </dependency>
            <dependency>
                <groupId>com.squareup.okio</groupId>
                <artifactId>okio</artifactId>
                <version>${okio.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
